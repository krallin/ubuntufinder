============
Installation
============

At the command line::

    $ easy_install ubuntufinder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ubuntufinder
    $ pip install ubuntufinder